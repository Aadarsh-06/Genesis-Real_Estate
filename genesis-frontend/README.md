# Genesis Frontend

This is a Vite + React + Tailwind frontend for the Genesis RAG system.

To run locally:

1. npm install
2. npm run dev

The frontend expects the backend at http://127.0.0.1:8000/ask
